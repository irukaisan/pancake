/* ============================================================
   TAKANENOSHI PANCAKE - animation.js
   AOS 初期化
   ============================================================ */

$(function () {
  AOS.init({
    duration: 700,
    easing: 'ease-out-cubic',
    once: true,
    offset: 60,
    delay: 0
  });
});
